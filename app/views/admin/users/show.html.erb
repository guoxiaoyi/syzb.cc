<p id="notice"><%= notice %></p>

<table class="table table-striped">
  <tr>
    <td><%= User.human_attribute_name(:name) %></td>
    <td><%= @user.name%></td>
  </tr>
  <tr>
    <td><%= User.human_attribute_name(:phone) %></td>
    <td><%= @user.phone%></td>
  </tr>
  <tr>
    <td><%= User.human_attribute_name(:manager_id) %></td>
    <td><%= @user.manager.name %></td>
  </tr>
  <tr>
    <td> <%= User.human_attribute_name(:levels) %> </td>
    <td><%= @user.current_levels%></td>
  </tr>
  <tr>
    <td> <%= User.human_attribute_name(:parent_phone) %> </td>
    <td> <%= @user.parent.name -%></td>
  </tr>
  <tr>
    <td> <%= User.human_attribute_name(:birthday) %> </td>
    <td> <%= @user.birthday %> </td>
  </tr>
  <tr>
    <td> <%= User.human_attribute_name(:balance) %></td>
    <td> <%= @user.balance %> </td>
  </tr>
</table>

<%= link_to t('buttons.edit'), edit_admin_user_path(@user) %> |
<%= link_to t('buttons.back'), admin_users_path %>
