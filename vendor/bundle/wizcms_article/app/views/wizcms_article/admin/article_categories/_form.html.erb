<%= simple_form_for([:admin, @article_category]) do |f| %>
  <%= f.error_notification %>
    <%= f.input :name %>
    <%= f.input :custom_order %>
    <%#= f.input :parent_id, collection: WizcmsArticle::ArticleCategory.tree.unshift(WizcmsArticle::ArticleCategoryRoot.new), label_method: :tree_select_name, prompt: "请选择父类别" %>
    <%#= f.input :cite_key, hint: '用于引用 例如：case news inside_news'%>
    <%#= f.input :brief %>
    <%#= f.input :published %>
    <div class="ln_solid"></div>
    <div class="item form-group">
      <div class="col-md-6 col-sm-6 offset-md-3">
        <%= link_to t('buttons.back'), admin_article_categories_path, class: 'btn btn-secondary' %>
        <%= f.button :submit, class: 'btn btn-success' %>
      </div>
    </div>

<% end %>

